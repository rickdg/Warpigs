# Workspace Rules

- **Track JSON files in GitHub**: JSON configuration and data files (e.g., `schedule.json`, `venues_info.json`, `contact.json`, `story.json`) are allowed to be tracked and committed to GitHub to enable local/remote editing synchronization.
