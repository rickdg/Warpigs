# Workspace Rules

- **Do NOT commit or upload JSON files to GitHub**: Whenever committing, staging, or pushing to GitHub, do not include any `.json` files (e.g., `venues.json`, `contact.json`, `cities.json`, `schedule.json`, `story.json`, `venues_info.json`). These files should remain strictly local and not be tracked or pushed.
